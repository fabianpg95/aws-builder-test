AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Ultimate SmartUploader Stack
  - Cognito User Pool and App Client
  - API Gateway REST with Cognito Authorizer
  - Lambdas (API handler and worker)
  - S3 Bucket for file storage (external bucket used)
  - DynamoDB for metadata
  - SNS Topic and SQS Queue
  - Minimal IAM Roles
  - CloudWatch Logs and X-Ray enabled
  - Parameters for Secrets Manager (placeholder)
  # Note: Aurora Serverless, Step Functions, CodePipeline, and CloudFront should be added separately.

Parameters:
  CognitoUserPoolName:
    Type: String
    Default: SmartUploaderUserPool
  CognitoAppClientName:
    Type: String
    Default: SmartUploaderAppClient
  S3BucketName:
    Type: String
    Default: smartuploader-code-bucket
  DynamoDBTableName:
    Type: String
    Default: SmartUploaderMetadata
  SNSTopicName:
    Type: String
    Default: SmartUploaderTopic
  SQSQueueName:
    Type: String
    Default: SmartUploaderQueue
  LambdaApiFunctionName:
    Type: String
    Default: SmartUploaderApiLambda
  LambdaWorkerFunctionName:
    Type: String
    Default: SmartUploaderWorkerLambda

Resources:

  # -------------------
  # Cognito User Pool
  # -------------------
  CognitoUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Ref CognitoUserPoolName
      AutoVerifiedAttributes:
        - email
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: false

  CognitoUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: !Ref CognitoAppClientName
      UserPoolId: !Ref CognitoUserPool
      GenerateSecret: false
      ExplicitAuthFlows:
        - ALLOW_USER_PASSWORD_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH
        - ALLOW_CUSTOM_AUTH
        - ALLOW_USER_SRP_AUTH

  # -------------------
  # S3 Bucket (COMMENTED OUT — bucket already exists)
  # -------------------
  # UploadsBucket:
  #   Type: AWS::S3::Bucket
  #   Properties:
  #     BucketName: !Ref S3BucketName
  #     VersioningConfiguration:
  #       Status: Enabled
  #     PublicAccessBlockConfiguration:
  #       BlockPublicAcls: true
  #       BlockPublicPolicy: true
  #       IgnorePublicAcls: true
  #       RestrictPublicBuckets: true

  # -------------------
  # DynamoDB Table
  # -------------------
  MetadataTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref DynamoDBTableName
      AttributeDefinitions:
        - AttributeName: file_id
          AttributeType: S
      KeySchema:
        - AttributeName: file_id
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST

  # -------------------
  # SNS Topic
  # -------------------
  UploadsSNSTopic:
    Type: AWS::SNS::Topic
    Properties:
      TopicName: !Ref SNSTopicName

  # -------------------
  # SQS Queue
  # -------------------
  UploadsSQSQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Ref SQSQueueName
      VisibilityTimeout: 60
      ReceiveMessageWaitTimeSeconds: 20

  # -------------------
  # IAM Roles
  # -------------------
  ApiLambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${LambdaApiFunctionName}-exec-role"
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: ApiLambdaPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:GetObject
                Resource: !Sub "arn:aws:s3:::${S3BucketName}/*"
              - Effect: Allow
                Action:
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                Resource: !GetAtt MetadataTable.Arn
              - Effect: Allow
                Action:
                  - sns:Publish
                Resource: !Ref UploadsSNSTopic

  WorkerLambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${LambdaWorkerFunctionName}-exec-role"
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: WorkerLambdaPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:UpdateItem
                Resource: !GetAtt MetadataTable.Arn
              - Effect: Allow
                Action:
                  - sqs:ReceiveMessage
                  - sqs:DeleteMessage
                  - sqs:GetQueueAttributes
                Resource: !GetAtt UploadsSQSQueue.Arn

  # -------------------
  # Lambda Functions
  # -------------------
  ApiLambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Ref LambdaApiFunctionName
      Runtime: python3.9
      Handler: api.lambda_function.lambda_handler
      Role: !GetAtt ApiLambdaExecutionRole.Arn
      Code:
        S3Bucket: smartuploader-code-bucket
        S3Key: api-lambda-code.zip
      Environment:
        Variables:
          BUCKET: !Ref S3BucketName
          TABLE: !Ref DynamoDBTableName
          TOPIC_ARN: !Ref UploadsSNSTopic
      TracingConfig:
        Mode: Active

  WorkerLambdaFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Ref LambdaWorkerFunctionName
      Runtime: python3.9
      Handler: workers.processor_lambda.lambda_handler
      Role: !GetAtt WorkerLambdaExecutionRole.Arn
      Code:
        S3Bucket: smartuploader-code-bucket
        S3Key: api-lambda-code.zip
      Environment:
        Variables:
          TABLE: !Ref DynamoDBTableName
      TracingConfig:
        Mode: Active

  # Lambda SQS Trigger
  WorkerLambdaSQSTrigger:
    Type: AWS::Lambda::EventSourceMapping
    Properties:
      BatchSize: 10
      EventSourceArn: !GetAtt UploadsSQSQueue.Arn
      FunctionName: !Ref WorkerLambdaFunction
      Enabled: true

  # -------------------
  # API Gateway REST API
  # -------------------
  ApiGatewayRestApi:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: SmartUploaderAPI

  ApiGatewayResourceUpload:
    Type: AWS::ApiGateway::Resource
    Properties:
      ParentId: !GetAtt ApiGatewayRestApi.RootResourceId
      PathPart: upload
      RestApiId: !Ref ApiGatewayRestApi

  ApiGatewayMethodPostUpload:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref ApiGatewayRestApi
      ResourceId: !Ref ApiGatewayResourceUpload
      HttpMethod: POST
      AuthorizationType: COGNITO_USER_POOLS
      AuthorizerId: !Ref ApiGatewayAuthorizer
      Integration:
        IntegrationHttpMethod: POST
        Type: AWS_PROXY
        Uri: !Sub
          - arn:aws:apigateway:${Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations
          - {
              Region: !Ref "AWS::Region",
              LambdaArn: !GetAtt ApiLambdaFunction.Arn
            }

  ApiGatewayAuthorizer:
    Type: AWS::ApiGateway::Authorizer
    Properties:
      Name: CognitoAuthorizer
      Type: COGNITO_USER_POOLS
      IdentitySource: method.request.header.Authorization
      RestApiId: !Ref ApiGatewayRestApi
      ProviderARNs:
        - !GetAtt CognitoUserPool.Arn

  LambdaPermissionForApiGateway:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref ApiLambdaFunction
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${ApiGatewayRestApi}/*/POST/upload

  # -------------------
  # CloudWatch Log Groups
  # -------------------
  ApiLambdaLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${LambdaApiFunctionName}"
      RetentionInDays: 14

  WorkerLambdaLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${LambdaWorkerFunctionName}"
      RetentionInDays: 14

Outputs:
  ApiGatewayInvokeUrl:
    Description: "Invoke URL for API Gateway"
    Value: !Sub "https://${ApiGatewayRestApi}.execute-api.${AWS::Region}.amazonaws.com/prod/upload"
